{ "sqlView":
  {
	"name": "$!encoder.jsonEncode( ${sqlViewObject.name} )",
	"uid": "$!{sqlViewObject.uid}",
	"description": "$!encoder.jsonEncode( ${sqlViewObject.description} )",
	"sqlquery": "$!encoder.jsonEncode( ${sqlViewObject.sqlQuery} )"
  }
}